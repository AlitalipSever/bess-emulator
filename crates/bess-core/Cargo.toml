[package]
name = "bess-core"
description = "Deterministic simulation kernel for a synthetic grid-scale battery plant"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
